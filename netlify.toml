# https://docs.netlify.com/build/configure-builds/file-based-configuration
# The Next app lives in ./web — Netlify must build from that folder.
[build]
  base = "web"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
